ARMORY = (function ( oldNS ) {
  var oilBarrels = 1000;
  var catapults = ["a", "b", "c"];
  oldNS.assignCatapult = function (regiment) {
    //does some resupplying here
    //won't have access to private data from oldNS reference
  };
  return oldNS;
)( ARMORY );
/*KEEP FILES DATA CENTRIC*/

/*What is the term for adding or changing properties in a module after the module has already been built?
AUGMENTATION
*/
